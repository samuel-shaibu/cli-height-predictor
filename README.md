# Height Predictor CLI

A simple ML tool packaged via Docker.

## Usage

You don't need to install Python or clone the repo. Just have Docker installed.

```bash
docker run ghcr.io/samuel-shaibu/height-predictor:latest --weight 180
```
